export declare global {
  interface LLayouts {
    components: {
      header: {
        disclaimer: string;
        mySettings: string;
        logout: string;
      };
    };
    detailLayout: {
      navTabs: {
        toggle: {
          tooltip: {
            expand: string;
            collapse: string;
          };
        };
      };
    };
  }
}
