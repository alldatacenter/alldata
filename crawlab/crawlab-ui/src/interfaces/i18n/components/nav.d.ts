interface LComponentsNav {
  actions: {
    back: string;
    save: string;
  };
  tabs: {
    toggle: {
      collapse: string;
      expand: string;
    };
  };
  sidebar: {
    search: string;
  };
}
