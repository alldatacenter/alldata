import { EventLevel, EventStatus } from '@/types/enum'

export const PaginationData = {
  current: 1,
  pageSize: 50,
  total: 0,
  showSizeChanger: true,
  pageSizeOptions: ['10', '20', '50', '100'],
  hideOnSinglePage: true,
}

