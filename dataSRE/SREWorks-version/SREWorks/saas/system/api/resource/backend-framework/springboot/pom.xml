<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.alibaba.sreworks.helloworld</groupId>
  <artifactId>sreworks-helloworld</artifactId>
  <packaging>pom</packaging>
  <version>0.0.1-SNAPSHOT</version>

  <name>sreworks-helloworld</name>

  <description>springboot project</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <project.java.version>1.8</project.java.version>

    <!--spring-->
    <spring.boot.dependencies.version>2.3.9.RELEASE</spring.boot.dependencies.version>
    <spring.cloud.dependencies.version>Hoxton.SR10</spring.cloud.dependencies.version>

    <!-- alibaba-->
    <spring.cloud.alibaba.version>2.1.2.RELEASE</spring.cloud.alibaba.version>

    <!-- tesla-->
    <tesla-spring-boot-starter.version>2.1.6-SNAPSHOT</tesla-spring-boot-starter.version>

    <!-- other -->
    <lombok.version>1.18.8</lombok.version>
    <junit.version>4.12</junit.version>
    <nacos.version>1.1.3</nacos.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.dependencies.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring.cloud.dependencies.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- self project -->
      <dependency>
        <artifactId>sreworks-helloworld-api</artifactId>
        <groupId>com.alibaba.sreworks.helloworld</groupId>
        <version>0.0.1-SNAPSHOT</version>
      </dependency>

      <dependency>
        <artifactId>sreworks-helloworld-common</artifactId>
        <groupId>com.alibaba.sreworks.helloworld</groupId>
        <version>0.0.1-SNAPSHOT</version>
      </dependency>

      <dependency>
        <artifactId>sreworks-helloworld-domain</artifactId>
        <groupId>com.alibaba.sreworks.helloworld</groupId>
        <version>0.0.1-SNAPSHOT</version>
      </dependency>

      <dependency>
        <artifactId>sreworks-helloworld-server</artifactId>
        <groupId>com.alibaba.sreworks.helloworld</groupId>
        <version>0.0.1-SNAPSHOT</version>
      </dependency>


      <!-- alibaba & tesla -->
      <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        <version>${spring.cloud.alibaba.version}</version>
      </dependency>


      <!-- tesla dependency -->
      <dependency>
        <groupId>com.alibaba.tesla</groupId>
        <artifactId>tesla-elastic-job-spring-boot-starter</artifactId>
        <version>${tesla-spring-boot-starter.version}</version>
      </dependency>

      <dependency>
        <groupId>com.alibaba.tesla</groupId>
        <artifactId>tesla-webmvc-spring-boot-starter</artifactId>
        <version>${tesla-spring-boot-starter.version}</version>
      </dependency>

      <dependency>
        <groupId>com.alibaba.tesla</groupId>
        <artifactId>tesla-swagger-spring-boot-starter</artifactId>
        <version>${tesla-spring-boot-starter.version}</version>
      </dependency>

      <dependency>
        <groupId>com.alibaba.tesla</groupId>
        <artifactId>tesla-mybatis-spring-boot-starter</artifactId>
      </dependency>

      <!-- other -->
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok.version}</version>
      </dependency>

      <!-- duubo -->
      <dependency>
        <groupId>com.alibaba.nacos</groupId>
        <artifactId>nacos-client</artifactId>
        <version>${nacos.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring.boot.dependencies.version}</version>
        </plugin>


        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>2.22.2</version>
          <configuration>
            <skip>true</skip>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.8.1</version>
          <configuration>
            <source>${project.java.version}</source>
            <target>${project.java.version}</target>
            <encoding>${project.build.sourceEncoding}</encoding>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-antrun-plugin</artifactId>
          <version>1.8</version>
        </plugin>

      </plugins>
    </pluginManagement>
    <plugins>
      <!--  要将源码放上去 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.2.1</version>
        <configuration>
          <attach>true</attach>
        </configuration>
        <executions>
          <execution>
            <phase>compile</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

    <modules>
      <module>sreworks-helloworld-api</module>
      <module>sreworks-helloworld-server</module>
      <module>sreworks-helloworld-domain</module>
      <module>sreworks-helloworld-common</module>
      <module>sreworks-helloworld-start</module>
  </modules>
</project>
