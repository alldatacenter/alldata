
DROP TABLE IF EXISTS log;
CREATE TABLE log (x UInt8) ENGINE = CnchMergeTree ORDER BY x;
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (0);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (1);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (2);
SELECT * FROM log ORDER BY x;
DROP TABLE log;
CREATE TABLE log (x UInt8) ENGINE = CnchMergeTree ORDER BY x;
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (0);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (1);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (2);
SELECT * FROM log ORDER BY x;
DROP TABLE log;
CREATE TABLE log (x UInt8) ENGINE = CnchMergeTree ORDER BY x;
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (0);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (1);
SELECT * FROM log ORDER BY x;
INSERT INTO log VALUES (2);
SELECT * FROM log ORDER BY x;
DROP TABLE log;
