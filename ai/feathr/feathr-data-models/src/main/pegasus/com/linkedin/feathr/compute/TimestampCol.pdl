namespace com.linkedin.feathr.compute

/**
 * Representation of a timestamp column field
 */
record TimestampCol {
  /**
   * Timestamp column expression.
   */
  expression: string

  /**
   * Format of the timestamp, example - yyyy/MM/dd, epoch, epoch_millis
   */
  format: string
}