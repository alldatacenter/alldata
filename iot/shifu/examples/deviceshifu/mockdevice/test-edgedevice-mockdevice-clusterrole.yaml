apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: edgedevice-mockdevice-clusterrole
rules:
- apiGroups:
  - shifu.edgenesis.io
  resources:
  - edgedevices
  verbs: ["get", "update"]
