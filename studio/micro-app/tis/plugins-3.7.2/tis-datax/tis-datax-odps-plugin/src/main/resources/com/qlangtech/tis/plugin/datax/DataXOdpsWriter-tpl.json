{
    "name": "odpswriter",
    "parameter": {
        "project": "${writer.project}",
        "table": "${writer.table}",
        "column": [
#foreach($c in $writer.column) "$c" ,#end],
        "accessId": "${writer.accessId}",
        "accessKey": "${writer.accessKey}",
        "truncate": ${writer.truncate},
        "odpsServer": "${writer.odpsServer}",
        "tunnelServer": "${writer.tunnelServer}",
        "partitionTimeFormat": "${writer.partitionTimeFormat}"
    }
}
