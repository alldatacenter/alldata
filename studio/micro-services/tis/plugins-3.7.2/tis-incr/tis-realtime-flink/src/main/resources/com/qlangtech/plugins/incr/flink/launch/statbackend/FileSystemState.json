{
  "enableSavePoint": {
    "dftVal": true,
    "help": "支持任务执行savepoint，Flink任务管理器执行停机操作时会主动触发创建savepoint操作，存放位置为checkpointDir平行目录下的一个以时间戳为子目录下",
    "enum": [
      {
        "label": "是",
        "val": true
      },
      {
        "label": "否",
        "val": false
      }
    ]
  }
}
