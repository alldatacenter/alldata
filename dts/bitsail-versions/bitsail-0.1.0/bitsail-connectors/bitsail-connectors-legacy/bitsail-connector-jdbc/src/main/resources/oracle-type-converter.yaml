engine.type.to.bitsail.type.converter:
  - source.type: char
    target.type: string

  - source.type: varchar
    target.type: string

  - source.type: interval day
    target.type: string

  - source.type: interval year
    target.type: string

  - source.type: intervalds
    target.type: string

  - source.type: intervalym
    target.type: string

  - source.type: varchar2
    target.type: string

  - source.type: nchar
    target.type: string

  - source.type: nvarchar2
    target.type: string

  - source.type: long
    target.type: string

  - source.type: blob
    target.type: string

  - source.type: clob
    target.type: string

  - source.type: nclob
    target.type: string

  - source.type: string
    target.type: string

  - source.type: character
    target.type: string

  - source.type: number
    target.type: long

  - source.type: integer
    target.type: int

  - source.type: int
    target.type: int

  - source.type: smallint
    target.type: short

  - source.type: float
    target.type: double

  - source.type: double
    target.type: double

  - source.type: double precision
    target.type: double

  - source.type: numeric
    target.type: bigdecimal

  - source.type: decimal
    target.type: bigdecimal

  - source.type: real
    target.type: double

  - source.type: bit
    target.type: boolean

  - source.type: bool
    target.type: boolean

  - source.type: date
    target.type: date.date

  - source.type: timestamp
    target.type: date.datetime

  - source.type: timestamp with time zone
    target.type: date.datetime

  - source.type: timestamp with local time zone
    target.type: date.datetime

  - source.type: datetime
    target.type: date.datetime

  - source.type: blob
    target.type: bytes

  - source.type: bfile
    target.type: bytes

  - source.type: raw
    target.type: bytes

  - source.type: long raw
    target.type: bytes

  - source.type: rowid
    target.type: string

  - source.type: urowid
    target.type: string

  - source.type: xmltype
    target.type: string

  - source.type: binary_float
    target.type: float

  - source.type: binary_double
    target.type: double